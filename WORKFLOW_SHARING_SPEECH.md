# 发言稿：从 obmm 项目看人机协作的工程实践

各位好，今天我想分享的不是某一个具体 bug，也不是某一段代码，而是我们在 obmm 这个项目里逐步形成的一套人机协作工作模式。

这个项目的背景比较特殊：我们要在 UCX 的 UCT 层新增一个 `obmm` transport，用来支撑 MPI/HPC 应用；底层依赖一套本地开发环境没有的硬件能力；UCX 本身又是一个大量依赖宏、ops table、capability、wireup 选择逻辑的框架。换句话说，这个项目天然容易出现三类问题：第一，AI 根据经验编造不存在的硬件行为；第二，复制已有 transport 的结构时顺手复制了不适用的语义；第三，代码看起来实现了，但 UCX/UCP 因为某个 capability 或 address 过滤规则，完全不会选择它。

最后 V2 能跑通，并不是因为一开始就写对了所有代码，而是因为我们把协作方式从“让 AI 多写代码”调整成了“让 AI 帮我们管理证据、约束和诊断路径”。

## 一、我们的核心工作模式

我把这套模式概括成一句话：

**人在场景和事实边界上负责，AI 在代码检索、框架理解、方案推演和重复性验证上负责；双方共同维护一套可复用的工程事实源。**

具体来说，我们做了几件事。

第一，我们不把关键事实只留在聊天记录里。硬件环境、libobmm API、UCX transport 约束、禁止调用的函数、必须保留的能力边界，都被写进了 `AGENTS.md`、skills、`DESIGN.md` 和工程实践文档。这样后面每一次新任务，不是靠 AI 记忆，而是先回到事实源。

第二，我们强制“先定位，后修改”。遇到 UCX 报错时，先 grep 报错字符串，找到源码里真正 emit error 的地方；遇到调用栈时，从 UCP/UCT 选择路径往下追；遇到 hang 时，先看 progress、pending、backpressure，而不是先猜 fence 或硬件问题。

第三，我们要求 AI 参考已有 transport，但不能照搬语义。比如 mm/self transport 很有参考价值，因为它们告诉我们 UCX 的 class、ops table、pending、iface_query 应该怎么接；但 obmm 的内存语义和 mm 不一样，所以 rkey_ptr、CPU fence、same-host reachability 这些语义不能直接复制。

第四，我们把验证分层。先看 `ucx_info`、capability、symbol；再跑 UCT 层；再跑 MPI；最后跑 OSU 这种更接近真实压力的 benchmark。每一层失败，就回到这一层的选择逻辑，而不是直接怀疑所有东西。

第五，我们让工作流适配真实环境。本地没有 Linux build 环境、没有 obmm 硬件，就不浪费时间反复跑不可能成功的构建或硬件测试。本地做静态检查、代码审查、设计校验；真实节点上的 MPI/OSU 由人来跑，再把日志和调用栈反馈回来。

## 二、为什么这种模式有效

这个项目里有几个很典型的例子。

第一个例子是 cross-node MPI 失败。最开始我们围绕 AM_SYNC、is_reachable、版本不一致猜了很多方向，浪费了时间。后来回到正确方法：grep 报错字符串，读 `select.c` 和 address pack 逻辑，发现跨节点时 OMPI 会用 NET_ONLY 地址过滤，而 obmm 没有广告 `UCT_IFACE_FLAG_INTER_NODE`，所以根本没有被打包进远端地址。最后是一行 capability 修复。

这个例子说明：复杂框架里的错误经常不是你想象的“功能没实现”，而是“框架在更早的选择阶段就把你过滤掉了”。

第二个例子是 OSU 大消息 segfault。调用栈显示 UCP 走到了 `rkey_ptr` rendezvous 路径，然后 memcpy 到一个未映射地址。原因不是 memcpy 本身，而是 obmm 错误广告了从 sm transport 复制来的 `RKEY_PTR` 能力。sm 可以这么做，是因为它能 attach 对端进程共享内存；obmm 只 mmap 预导入的 OBMM region，不能访问任意 peer heap。修复就是删掉不真实的 capability，让 UCP 选择慢一点但正确的 AM 协议。

这个例子说明：在框架型项目里，广告一个你不真正支持的能力，比少实现一个能力更危险。

第三个例子是 OSU bibw hang。早期 pending_add 返回 `UCS_ERR_BUSY`，简单场景能跑，但双向窗口压力下 UCP 会在同一个调用栈里 busy-spin，不让 progress 执行，双方 FIFO 都无法 drain。最后我们按 mm 的 arbiter 模式实现真实 pending 队列，问题才解决。

这个例子说明：bring-up 阶段的简化策略，如果进入稳定基线，就会在真实 workload 下变成死锁。

第四个例子是 V3 hybrid 设计。我们最容易犯的错，是把 CC 当成“更快的普通共享内存”。但 obmm 的 cacheable memory 有 ownership 约束：同一时刻要么 all-read/none，要么 one-writer/all-none。如果把 FIFO/control 放在 CC 上，sender 写、receiver poll 这个基本模型就冲突了。因此 V3 最终设计成 hybrid：NC 负责 FIFO/control/am_short，CC 只承载 bcopy payload，并且每条 bcopy 消息都有明确的 ownership handoff。

这个例子说明：性能优化必须服从真实一致性模型；如果基础语义不成立，越优化越危险。

## 三、可迁移的经验

虽然 obmm 项目很特殊，但我们沉淀出来的经验是可以迁移的。

第一，**事实要资产化**。项目里所有会影响设计的事实，都应该进入文档、skill、ADR、测试说明或其他稳定位置。不要让关键事实只存在于某次会议或聊天上下文里。

第二，**诊断要证据化**。不要问“可能是什么原因”，先问“系统在哪里做出了这个决定”。错误字符串、调用栈、日志、capability、配置、协议选择路径，都是证据入口。

第三，**参考实现只能复制结构，不能复制承诺**。框架的生命周期、宏、hook、ops table 可以参考；但能力、内存语义、地址语义、错误恢复，必须重新证明。

第四，**metadata 要诚实**。很多系统都会先读 metadata 再选择路径，比如 capability、schema、manifest、feature flag、API version、配置开关。metadata 一旦撒谎，后面代码再正确也会被错误使用。

第五，**共享状态就是协议**。只要有两个进程、两个节点、两个版本、两个服务会读同一段状态，就要把 layout、version、ownership、lifetime、rollback 写清楚。

第六，**测试要分层**。不要把 benchmark 当诊断。benchmark 只能告诉我们“真实压力下失败了”，但 root cause 通常在更底层的选择逻辑、队列、资源、协议或能力广告里。

第七，**验证要适配环境**。如果当前环境没有某个依赖，就不要让 AI 反复尝试同一个失败命令。应该把验证分成：本地可验证、build host 可验证、真实环境可验证、暂时不可验证。

第八，**并行适合检索，不适合拆散强耦合设计**。md、iface、ep、caps、address、wire format 这种必须全局一致的改动，最好由一个连续的上下文完成；可以让 sub-agent 做研究、审查、构建日志分析，但不要把同一个协议拆成几个互相不知道的人分别写。

## 四、我们如何推广这套模式

如果要把这套经验推广到其他项目，我建议不是照搬 obmm 的文档结构，而是推广四个动作。

第一，为项目建立一份“事实源”。里面写清楚环境、边界、禁止事项、外部系统真实行为、已经验证过的命令、不能本地验证的部分。

第二，为常见任务建立“工作流”。比如调试工作流、代码变更工作流、build/verify 工作流、review 工作流。工作流不要太复杂，但要能阻止最常见的错路。

第三，把失败案例抽象成规则。不要只记录“某次 bug 怎么修”，而要问：这次 bug 背后的通用原则是什么？例如“先 grep 报错字符串”，“不要广告不真实能力”，“本地无法验证就显式 handoff”。

第四，让 AI 承担它擅长的部分：快速阅读大量代码、交叉引用框架路径、生成候选方案、做一致性检查、整理文档和复盘。让人承担它不擅长也不该擅长的部分：真实环境事实、业务优先级、硬件约束、最终验收。

## 五、结尾

我认为这次项目最有价值的地方，不是我们让 AI 写了多少代码，而是我们逐步建立了一种更可靠的人机协作方式。

AI 很适合加速复杂系统里的搜索、归纳和实现，但它也很容易在缺少事实时“合理地编造”。我们的做法，是用事实源、工作流、分层验证和复盘机制，把 AI 的速度保留下来，把幻觉和无用功压下去。

这套模式并不只适用于 UCX，也不只适用于硬件相关项目。任何复杂框架、分布式系统、存储系统、编译器、基础设施、业务平台，只要存在“局部代码看起来对，但系统选择路径很复杂”的问题，都可以复用类似方法。

最后用一句话总结：

**好的 AI 协作不是让 AI 直接替我们判断，而是让 AI 帮我们更快地找到证据、维护约束，并在正确的边界内完成工程实现。**
