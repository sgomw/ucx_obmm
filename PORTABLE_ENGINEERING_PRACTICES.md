# 可迁移工程实践总结

这份文档抽象自 obmm/UCX 项目的经验，但不依赖该项目背景。它适用于任何高复杂度、强框架约束、测试环境受限、容易因错误假设而走偏的软件工程任务。

## 1. 先建立事实源，再建立方案

复杂项目失败往往不是因为代码写不出来，而是因为事实没有被稳定管理。开始实现前，应先把以下内容固化成可反复读取的事实源：

- 目标环境：运行平台、部署拓扑、权限、可用工具、不可用工具。
- 框架契约：注册点、生命周期、hook、capability、配置项、错误处理要求。
- 外部 API 语义：哪些 API 可调用，哪些 API 禁止调用，哪些行为不能假设。
- 已验证边界：哪些测试已通过，哪些只能由用户/硬件环境验证。
- 非目标：明确当前阶段不做什么，防止功能蔓延。

抽象规则：**不要让关键约束只存在于聊天上下文或个人记忆里。**

## 2. 诊断优先于修改

遇到错误时，先定位事实，再动代码：

1. 字符串错误：搜索完整错误字符串，找到产生位置。
2. 调用栈错误：从栈顶框架函数向下追到选择该路径的条件。
3. hang：先查 progress、队列、资源归还、锁/ownership、背压路径。
4. crash：先查是否选择了不真实广告的能力或不合法的内存语义。

抽象规则：**不要在没有定位 emit site / precondition 前列假设清单。**

## 3. 框架代码只能“借结构”，不能“借语义”

参考已有模块是正确的，但要区分两类内容：

| 可以复用 | 必须重新证明 |
| --- | --- |
| class/ops 注册结构 | capability 是否真实 |
| 生命周期钩子模式 | 内存/网络/设备语义 |
| pending/progress 框架写法 | 地址是否在本进程可访问 |
| 配置表和查询接口形态 | 错误恢复是否适合当前系统 |

抽象规则：**复制框架骨架，重新证明语义前提。**

## 4. 能力广告必须等于真实能力

在任何框架中，capability、feature flag、protocol option、配置开关都可能驱动上层选择完全不同的路径。错误广告比少广告更危险。

实践要求：

- 每个 advertised capability 都要能指出对应实现。
- 每个 numeric cap 都要和真实 buffer/协议/生命周期一致。
- 每个 unsupported feature 都要明确拒绝，而不是假装支持。
- 一旦发现上层选择了错误协议，优先检查 capability 与真实语义是否一致。

抽象规则：**宁可慢而正确，不要快而虚假。**

## 5. 分层验证，不跨层猜因果

复杂系统应按层验证：

1. 静态检查：格式、符号、调用点、配置键。
2. 单组件验证：绕开上层，验证模块基本行为。
3. 框架集成验证：验证注册、选择、capability、地址交换。
4. 应用级验证：真实 workload、压力、并发、边界尺寸。
5. 长尾验证：benchmark、异常路径、恢复路径。

失败时只解释当前层的前置条件，不把高层失败直接归因到低层硬件或业务逻辑。

抽象规则：**一层一层缩小问题，不跨层跳结论。**

## 6. 把“不可验证”当成一等状态

很多工程任务没有完整本地环境。此时不要伪造验证结果，也不要反复尝试不可能成功的命令。

应明确区分：

- 本地已验证。
- 构建机已验证。
- 用户/硬件环境已验证。
- 只能静态审查，尚未运行验证。
- 当前环境无法验证。

抽象规则：**不可验证不是失败，但假装验证是失败。**

## 7. 设计变更先改文档，再改 wire/API

以下变更必须先写设计，再实现：

- wire format / schema / DB migration / protocol layout
- capability / feature flag
- address / identity / reachability
- resource lifecycle / ownership / locking
- queue / backpressure / retry / pending
- cross-process or cross-node synchronization

设计不需要冗长，但必须写清楚：不变量、状态机、失败路径、兼容性、验证方法。

抽象规则：**会影响多处一致性的变更，先写不变量。**

## 8. 保持稳定基线

当一个阶段已经成功，应把它定义为 regression boundary：

- 默认路径保持兼容。
- 新模式通过配置或明确入口启用。
- 新逻辑不要污染已验证路径。
- 先证明“不破坏旧路径”，再证明“新路径有效”。

抽象规则：**阶段性成功不是终点，是下一阶段的保护边界。**

## 9. 识别无用功触发器

出现以下情况时应停下：

- 在未读源码前解释框架错误。
- 反复运行当前环境不具备条件的命令。
- 为受限运行环境编写依赖复杂工具链的脚本。
- 在根因不明时做大范围重构。
- 把性能问题提前到协议正确性之前。
- 把强耦合模块拆给多个低上下文执行者并行修改。
- 为了“看起来支持”而打开 capability/feature flag。

抽象规则：**无证据的行动越快，返工越多。**

## 10. 人机协作分工

在硬件或生产环境不可本地复现时，应明确分工：

- Agent/工程师：读代码、定位条件、设计最小修改、写可运行命令。
- 用户/环境 owner：提供真实运行结果、硬件事实、权限/部署约束。
- 文档/技能/记忆：保存已经确认的事实和抽象规则。

抽象规则：**不能访问环境的一方不能编造环境；能访问环境的一方应提供可定位证据。**

## 11. 最小可迁移工作流

可在其他项目直接复用：

1. 建立事实源：环境、框架契约、外部 API、非目标。
2. 定义稳定基线：当前已工作的能力和不可破坏边界。
3. 做代码检索：先找参考实现，再读精确源码。
4. 写短设计：不变量、状态机、失败路径、验证方式。
5. 批判设计：找 silent mismatch、资源泄漏、兼容性和不可验证假设。
6. 小步实现：保持 ops/caps/config/address/docs 同步。
7. 分层验证：本地静态、构建、组件、集成、真实 workload。
8. 复盘固化：把事实写入项目规则，把抽象经验写入可迁移实践。

一句话总结：**事实先于方案，定位先于修改，能力必须真实，验证必须分层，成功基线必须保护。**
