1. 验证发现 & 能力

 # obmm 设备出现，type 标对（mem 或 sw）
 ucx_info -d | grep -A6 obmm
 
 # 看 obmm 的 caps：必须出现 am_short，max_short ≈ 2032，CONNECT_TO_IFACE，CB_SYNC，没有 EP_CHECK
 ucx_info -d -u t -t obmm

不通过先停下，没必要往后跑。

2. 强制只走 obmm（避免 sm/tcp/self 兜底）

 export UCX_TLS=obmm
 export UCX_LOG_LEVEL=info        # 看 wireup 选择
 export UCX_PROTO_INFO=y          # 看 active message 实际落在哪个 lane

3. perftest（UCP 路径，最贴近 MPI 实际栈）

终端 A（server）：

 ucx_perftest -t am_lat -m host

终端 B（client，同一节点）：

 ucx_perftest 127.0.0.1 -t am_lat -m host -n 100000 -s 8

观察点：

 - A 启动时 info 日志里要有 obmm/... 被 select
 - B 跟 A 建链时 PROTO_INFO 那段，AM lane 标的是 obmm/...
 - 跑通后再扫尺寸：-s 8 / 64 / 256 / 1024 / 2000（<= max_short 都得过；> max_short 由于我们没实现 
bcopy/zcopy，会失败或 fallback —— 跑到 fallback 就说明 TLS 被破了）

4. 带宽（顺手验 FAA + owner-bit toggling 的稳态行为）

 # server
 ucx_perftest -t am_bw -m host
 # client
 ucx_perftest 127.0.0.1 -t am_bw -m host -n 1000000 -s 256

如果跑着跑着卡住或者吞吐崩，多半是 owner-bit 的 lap 极性算错，或者 generation 漂移把正常包当 stale 丢了 —— 
这两个点是最有可能出 bug 的地方，跑完贴日志我看。

5. 多对（验 slot 分配 / scavenge）

 # 同时起两份 server/client 对（各自 -p 端口）
 ucx_perftest -t am_lat -p 13337 &
 ucx_perftest -t am_lat -p 13338 &
 ucx_perftest 127.0.0.1 -t am_lat -p 13337 -n 50000 -s 8 &
 ucx_perftest 127.0.0.1 -t am_lat -p 13338 -n 50000 -s 8 &
 wait

 - pool 应该有 4 个 slot 被占用（2 server + 2 client），都跑完后正常释放
 - 然后再跑一次第 3 步，应当还能正常占到 slot（验证 free path）

6. 死亡恢复（验 scavenge）

跑一对 server/client，server 起来后用 kill -9 干掉，立刻再起一个新 server。如果新 server 报 NO_RESOURCE，说明 
scavenge 路径有 bug；如果秒起，说明
owner_pid + starttime 的探活生效了。

-------------------------------------------------------------------------------------------------------------------

踩坑预警（你跑的时候多半会撞到的）：

 1. UCX_TLS=obmm 单独设可能让 UCP 找不到 self/control 通路而 wireup 失败。如果第 3 步 server 起不来，临时换 
UCX_TLS=obmm,self，再确认 PROTO_INFO 里数据 lane 是 obmm，不是 self。
 2. perftest 默认会把 active message size 带 wire header 算进去，给 -s 留点 margin（≤ 2000 比较稳）。
 3. 看到 ucx_perftest 报 unsupported，多半是它默认想用 tag/RMA，确认你用的是 -t am_lat / am_bw，不是 -t tag_lat。

跑完贴 perftest 输出 + 任何 warn/error 日志给我，重点看延迟数字是否合理（同节点 NC 映射，应该几个微秒级，比 mm 
慢但不会差一个数量级）。
