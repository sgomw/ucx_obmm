# OBMM ucx_perftest 测试命令清单

> 适用：UCT 层 perftest，单节点自环 + 双节点。
> 关键：am_lat / am_bw 是 UCT 测试，必须 -d memory -x obmm。

---

## 0. 环境变量（可选）

```bash
export UCX_LOG_LEVEL=info
# 出问题时再开 debug：export UCX_LOG_LEVEL=debug
```
注意：UCX_TLS / UCX_PROTO_INFO 是 UCP 层变量，UCT perftest 不读取，不必设。

---

## 1. 发现 / 能力检查

```bash
ucx_info -d | grep -A6 obmm
ucx_info -d -u t -t obmm
```
预期 caps：am_short、CONNECT_TO_IFACE、CB_SYNC，max_short ≈ 2032。

---

## 2. am_lat（单节点自环）

server：
```bash
ucx_perftest -d memory -x obmm -t am_lat
```
client：
```bash
ucx_perftest 127.0.0.1 -d memory -x obmm -t am_lat -n 100000 -s 8
```

尺寸扫描：-s 8 / 64 / 256 / 1024 / 2000，全部 <= max_short 都应通过。

---

## 3. am_bw

```bash
# server
ucx_perftest -d memory -x obmm -t am_bw
# client
ucx_perftest 127.0.0.1 -d memory -x obmm -t am_bw -n 1000000 -s 256
```

关注：吞吐稳定、不卡死、不丢包；查日志中 "drop stale elem" 类 trace。

---

## 4. 多对并发（slot 分配/复用）

```bash
ucx_perftest -d memory -x obmm -t am_lat -p 13337 &
ucx_perftest -d memory -x obmm -t am_lat -p 13338 &
sleep 1
ucx_perftest 127.0.0.1 -d memory -x obmm -t am_lat -p 13337 -n 50000 -s 8 &
ucx_perftest 127.0.0.1 -d memory -x obmm -t am_lat -p 13338 -n 50000 -s 8 &
wait
```

跑完再回 §2 重跑一次，slot 仍能正常分到 = free 路径 OK。

---

## 5. 死亡恢复（scavenge）

```bash
# 终端 A
ucx_perftest -d memory -x obmm -t am_lat
# 终端 C
pkill -9 -f 'ucx_perftest.*am_lat'
# 终端 A 立刻再起
ucx_perftest -d memory -x obmm -t am_lat
```

新 server 应秒起；NO_RESOURCE = scavenge 路径有 bug。

---

## 6. 双节点

节点 0：
```bash
ucx_perftest -d memory -x obmm -t am_lat
```
节点 1：
```bash
ucx_perftest <node0_ip> -d memory -x obmm -t am_lat -n 100000 -s 8
```

---

## 已知踩坑

1. UCT 测试（am_lat / am_bw / put_lat / get_lat）必须 -d <dev> -x <tl>。
2. -m host 是 UCP 测试参数，UCT 测试不需要，传了会报参数错。
3. -s 太靠近 max_short 会因 wire header + elem header 余量不足报 INVALID_PARAM，留 ≥ 24B margin。
4. /dev/obmm_shmdev* 报 EPERM：检查 export 端是否带了允许 NC 共享模式的 flag。
